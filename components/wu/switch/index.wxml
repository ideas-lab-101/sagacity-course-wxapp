<view class="wux-class {{ classes.wrap }}">
    <view class="{{ classes.input }} {{ size === 'small'?'small':''}} {{ size === 'mini'?'mini':''}}" style="{{ inputChecked ? inputStyle : '' }}" bindtap="onTap"></view>
</view>
